// Rabbit can generate C++ libraries, when it does it generates include header files for all types in the following format: typename.rbt.h
// That header file wraps the rabbit type using the pimpl pattern (privte pointer implementation).
#include ""
