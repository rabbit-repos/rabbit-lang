#include "StringBlob.h"

// size_t StringLength(const Char * aString)
// {
// 	size_t c = 0;
// 	while (aString)
// 	{
// 		aString++;
// 		c++;
// 	}
// 	return c;
// }

// TODO: Implement
void StringBlob::Reserve(const i32 /* aLength */)
{
	// Char * oldData = myData;
	// 
	// myData = static_cast<Char*>(malloc(aLength + 1));
	// if (!myData)
	// 	abort();
	// 
	// if (oldData)
	// {
	// 	memcpy(myData, oldData, sizeof Char * myLength + 1);
	// 	free(oldData);
	// }
}
